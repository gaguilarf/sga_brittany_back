name: Deploy to Production

on:
  push:
    branches:
      - main  # Se ejecuta automáticamente al hacer push a main
  workflow_dispatch:  # También permite ejecución manual desde GitHub

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
      
      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
      
      - name: 📦 Install dependencies
        run: npm ci --legacy-peer-deps
      
      - name: 🔨 Build TypeScript to JavaScript
        run: npm run build
      
      - name: 📁 Prepare deployment package (without node_modules)
        run: |
          mkdir -p deploy
          cp -r dist deploy/
          cp package.json deploy/
          cp package-lock.json deploy/
          cp .env.production deploy/.env.production
          cp start.sh deploy/
          cp stop.sh deploy/
          cp ecosystem.config.json deploy/
          echo "✅ Deployment package created (node_modules will be installed on server)"
          
      - name: 🚀 Upload to server via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASS }}
          local-dir: ./deploy/
          server-dir: ./
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/*.md
          dangerous-clean-slate: false
          timeout: 300000
      
      - name: ✅ Deployment Summary
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "✅ FILES UPLOADED SUCCESSFULLY!"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "🌐 API URL: https://api.brittanygroup.edu.pe/api"
          echo "📚 Swagger: https://api.brittanygroup.edu.pe/api/docs"
          echo ""
          echo "⚠️  REQUIRED STEPS ON THE SERVER:"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""
          echo "1. SSH into your server"
          echo ""
          echo "2. Navigate to api directory:"
          echo "   cd /api.brittanygroup.edu.pe/"
          echo ""
          echo "3. Install dependencies (REQUIRED):"
          echo "   npm ci --omit=dev --legacy-peer-deps"
          echo ""
          echo "4. If first deployment:"
          echo "   - cp .env.production .env"
          echo "   - nano .env  # Edit with real credentials"
          echo "   - chmod +x start.sh stop.sh"
          echo "   - npm install -g pm2"
          echo "   - pm2 start ecosystem.config.json"
          echo "   - pm2 save"
          echo ""
          echo "5. If updating existing deployment:"
          echo "   - pm2 restart brittany-api"
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "⚡ Note: node_modules are NOT uploaded via FTP"
          echo "   They must be installed on the server (step 3)"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"



